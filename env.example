# --- Vercel Environment Variables (Project Settings) ---
# 数据库连接串 (Transaction Mode for Serverless)
# 获取方式: Supabase Dashboard -> Connect -> Transaction Pooler
DATABASE_URL="postgres://postgres.xxxx:password@aws-0-region.pooler.supabase.com:6543/postgres?pgbouncer=true"

# 数据库直连 (用于 Migration)
# 获取方式: Supabase Dashboard -> Connect -> Session Pooler
DIRECT_URL="postgres://postgres.xxxx:password@aws-0-region.pooler.supabase.com:5432/postgres"

# Better Auth 密钥
# 生成方式: openssl rand -base64 32
BETTER_AUTH_SECRET="REPLACE_WITH_LONG_RANDOM_SECRET"

# 应用域名配置
# Preview 环境: Vercel 会自动分配，或手动填分配后的域名 (如 https://n-back-git-v2-dev-xxx.vercel.app)
# Production 环境: https://your-domain.com
BETTER_AUTH_URL="https://brain-training-system-git-v2-dev-qcbs-projects.vercel.app"
NEXT_PUBLIC_BASE_URL="https://brain-training-system-git-v2-dev-qcbs-projects.vercel.app"

# 前端 Auth API 地址 (Vite 代理需要，但 Vercel 上通常不需要配置此项，因为是同域)
# VITE_AUTH_BASE_URL="/api/auth"

# Google OAuth (可选，Phase 7)
# GOOGLE_CLIENT_ID=""
# GOOGLE_CLIENT_SECRET=""

# Resend (邮件发送，可选)
# 需要在 Resend 控制台创建 API Key
# RESEND_API_KEY=""
# 发件人地址（需在 Resend 验证域名/地址）
# RESEND_FROM_EMAIL=""
